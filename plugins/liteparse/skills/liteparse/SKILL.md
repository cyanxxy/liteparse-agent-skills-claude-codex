---
name: liteparse
description: Use when looking up LiteParse v2 CLI flags, dependencies, or JSON output shape.
---

# LiteParse Reference

Use [the CLI reference](references/cli-reference.md) for details not already in an action skill:

- binary and `npx` fallback behavior,
- flags for `parse`, `batch-parse`, and `screenshot`,
- LibreOffice/ImageMagick dependencies,
- JSON output shape,
- input formats and common failure modes.

Let the invoked action skill drive the workflow; this skill is background reference only.
